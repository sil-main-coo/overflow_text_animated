name: overflow_text_animated
description: A Flutter package project that handles text overflow with animation.
version: 0.0.5
homepage: https://github.com/sil-main-coo/overflow_text_animated
maintainer: Truong Viet Hoang (@sil-main-coo)

environment:
  sdk: '>=2.17.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0